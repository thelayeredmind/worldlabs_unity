# morphing — Session 7 (2026-07-16)

[^] Resuming morphing. Context's note that the symmmetric-greedy+gale merge was "uncommitted" is stale — git log shows it landed (8deeeed merge, 029edc4 wrap-up), working tree clean on main. No open session existed prior to this; starting fresh in Drift.

- Regression: building a MorphMap between a very big splat and a very small one (built "somewhere during the day" as an experiment) produces a broken transition — rainbow-colored quads. Reported as a bug to fix.
- Open question: for the more complex correspondence algorithms (mutual top-K, GS-Matching, etc.), are splats being spatially sorted/bucketed *before* running these algorithms, or not? User wants to investigate this as a possible alternative/complementary method.
- UI cleanup goal: GaussianMorphMapBuilderWindow has accumulated too many checkboxes and dead branches. Specifically called out: GPU Remainder Resolution toggle is gone/dead, too many diagnostic buttons that seem superseded by later ones.
- Bug: the match/unmatched readout on GaussianSplatMorpherEditor (added session 6) has a display bug.

- [D] User chose UI cleanup as the first thread to sift into an Activity, ahead of the rainbow-quad regression and the spatial-sorting question.

[Activity] Survey current state of GaussianMorphMapBuilderWindow.cs and GaussianSplatMorpherEditor.cs to understand what's actually there before sifting UI cleanup into concrete steps.

- [F] ~70% confidence: "Remainder Resolution is gone" doesn't match what's in the file — `m_UseRemainderFallback` (line 30) and its toggle (line 188-190) still exist, just disabled (grayed out) whenever spatial-probes or mutual-top-K is selected. Possible user meant something else (e.g. GS-Matching path has no remainder-fallback toggle at all — it always falls back internally per Context's Where section, no user-facing control) — needs the user's confirmation of the actual symptom.
- [F] Builder window has 5 top-level algorithm-selection toggles (spatial probes, mutual top-K, remainder fallback, GS-Matching) plus 6 diagnostic buttons (Sample Match Quality, Analyze Duplicates, Analyze Candidate Collisions, Top Duplicated R Splats, Verify Top-K Matches Kernel, Analyze Spatial Probe Occupancy) — all always visible regardless of which algorithm is active, none hidden/grouped by relevance.
- [F] GaussianSplatMorpherEditor's Matched/Unmatched readout (line 73-76): `total = Mathf.Max(currentMap.splatCountLeft, 1)` always uses the map's stored `splatCountLeft` as denominator, regardless of whether the map is actually oriented swapped relative to the morpher's current assetLeft/assetRight assignment (MapIsSwapped() exists elsewhere and handles this for GPU buffer setup, but this readout doesn't call it) — plausible source of the "bug" but not yet confirmed against the user's actual symptom.
- [R] Need user to confirm: (1) what exactly does "Remainder Resolution is gone" mean — button missing entirely, or something else? (2) what's the actual visual/numeric symptom on the Matched/Unmatched readout bug?

- User provided two screenshots: Morph Map Builder window (checkbox stack, Remainder Fallback IS checked/enabled — not missing), and GaussianSplatMorpherEditor showing "Matched / Unmatched: 344651 / 0 (131.5% matched)" with Asset Left=bonsai, Asset Right=car model, but assigned MorphMap is named "car model_bonsai_MorphMap" (built with opposite orientation).
- [F] ~90% confidence: confirmed the Matched/Unmatched bug — map's stored L/R orientation is swapped relative to the morpher's current Asset Left/Right assignment (map built car model→bonsai, morpher assigned bonsai→car model). Readout divides matched count by `splatCountLeft` without calling `MapIsSwapped()`, producing the impossible 131.5%. Same root cause the GUID-identity fix (session 5) solved for GPU buffer setup — this readout was never updated to use that same check.
- [D] This bonsai/car-model asset pair (very large vs very small splat) IS the same pairing behind the "rainbow colored quads" regression report — same MorphMap, same asset pair. The UI-bug thread and the regression thread are the same underlying case, just two different symptoms of it (wrong orientation handling).
- [F] ~60% confidence: "Remainder Resolution is gone" was a misread, not a missing feature — screenshot shows "Use remainder fallback" checked/enabled normally. Likely the real complaint is the checkbox stack's low visual differentiation (5 similarly-styled toggles stacked with inconsistent enabled/disabled greying) making it hard to tell what's actually active — a legibility/IA problem, not a removed feature.

- [D] User corrected: the Matched/Unmatched readout bug is UNRELATED to morph direction/interpolation, and is explicitly NOT responsible for the rainbow-quad rendering regression — these are two genuinely separate bugs, contrary to my ~90%/~60% guess last turn. Retracting that link.
- [D] UI redesign spec for GaussianMorphMapBuilderWindow, given directly by user:
  - Replace the four algorithm toggles (spatial probes, mutual top-K, GS-Matching, + implicit round-based baseline) with a single dropdown: "Correspondence Algorithm".
  - Replace `m_UseRemainderFallback` with one checkbox: "Force Match Pass" (new name/framing, not just a rename — needs to confirm exact semantics against current m_UseRemainderFallback behavior).
  - All diagnostic buttons (Sample Match Quality, Analyze Duplicates, Analyze Candidate Collisions, Top Duplicated R Splats, Verify Top-K Matches Kernel, Analyze Spatial Probe Occupancy) move under one foldable section: "Diagnostics".
  - Inside Diagnostics, split into two subsections: "Analyze Morphmap" and "Analyze Morph Candidates" — need to sort which existing button goes in which bucket.
- [R] New question raised: "Did we ever change the Interpolation algorithm?" — user suspects a regression may trace back to a change in the interpolation/morph-compute path itself (SplatMorph.compute or the playback blend), wants git history navigation to find the last known-good version. Explicitly deferred — "this is for later," not part of the current UI Activity.

- [D] User confirmed diagnostic bucket split: Analyze Morphmap = {Sample Match Quality, Analyze Duplicates, Top Duplicated R Splats} (operate on a saved GaussianMorphMap asset). Analyze Morph Candidates = {Analyze Candidate Collisions, Verify Top-K Matches Kernel, Analyze Spatial Probe Occupancy} (operate on raw asset pairs pre-build).
- [D] Additional spec detail: within Analyze Morph Candidates, buttons tied to a specific algorithm (e.g. "Analyze Spatial Probe Occupancy" only makes sense for the Spatial Probes algorithm) should only be shown/visible when that algorithm is the currently-selected one in the new Correspondence Algorithm dropdown — not just present-but-disabled, actually hidden. Verify Top-K Matches Kernel is algorithm-tied to Mutual Top-K (and possibly GS-Matching, since GS-Matching also uses FindTopKMatches-adjacent candidate generation — needs a check during implementation). Analyze Candidate Collisions uses DispatchCorrespondenceShader directly (the plain/round-based single-argmin kernel) — likely shown for the baseline "Round-based" algorithm choice, not algorithm-agnostic as previously assumed.

- [F] Confirmed by reading GaussianMorphMapBuilder.cs: BuildViaGaleShapley uses its own IGaleShapleyDispatcher targeting SplatGaleShapley.compute — never calls FindTopKMatches. Verify Top-K Matches Kernel is tied to Mutual Top-K only, not also GS-Matching.
[S] Implemented the UI redesign spec in GaussianMorphMapBuilderWindow.cs:
- Added `CorrespondenceAlgorithm` enum (RoundBased/SpatialProbes/MutualTopK/GaleShapley), replacing the 4 bool toggles (m_UseSpatialProbes/m_UseMutualTopK/m_UseGaleShapley) with one EnumPopup dropdown "Correspondence Algorithm". Probe Accuracy slider now shown only when SpatialProbes selected (was previously a disabled-but-visible slider).
- Renamed m_UseRemainderFallback -> m_ForceMatchPass, one checkbox "Force Match Pass", same semantics (fill remaining gaps via nearest-neighbor when on; report raw algorithm quality when off). Still only wired into the RoundBased/Build() path, matching prior behavior (Spatial Probes/Mutual Top-K never used it; GS-Matching has its own internal unconditional fallback).
- Added m_ShowDiagnostics foldout wrapping the old flat "Match Quality Sample"/"Spatial Probe Partitioning" sections, now split into two labeled subsections: "Analyze Morphmap" (Sample Match Quality, Analyze Duplicates, Top Duplicated R Splats — all operate on m_SampleMap) and "Analyze Morph Candidates" (Analyze Candidate Collisions shown only for RoundBased, Verify Top-K Matches Kernel shown only for MutualTopK, Analyze Spatial Probe Occupancy shown only for SpatialProbes) — algorithm-tied buttons are conditionally hidden (not just disabled) based on active dropdown selection, per user's explicit spec.
- StartBuild()'s dispatch switched from if/else-if on 4 bools to a switch on the enum; capture-and-pass-through pattern (assetLeft/colorWeight/etc captured before Task.Run) preserved as-is.
[/S]
[F] Verified via `unity_get_compilation_errors` against the TechTests Unity instance (port 7891) — 0 errors, compiles clean.

[^] Continue morphing. Last: implemented and verified (compiles clean, 0 errors) the full UI redesign spec for GaussianMorphMapBuilderWindow — Correspondence Algorithm dropdown, Force Match Pass checkbox, Diagnostics foldout with Analyze Morphmap / Analyze Morph Candidates subsections and per-algorithm conditional button visibility. Not yet tested live in the Editor GUI (only compile-verified) — should open the actual window and click through it before considering this Activity done. Next: user should visually verify the new window layout, then decide whether to move to the next thread (rainbow-quad regression investigation, or the deferred "did the interpolation algorithm change" git-archaeology question, or the spatial-sorting question). Confirm: none — implementation matches the fully-specified/confirmed design, no open assumptions left.
