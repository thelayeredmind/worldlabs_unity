# performance — Context

## What
Understanding the bottleneck(s) limiting Quest 3 Gaussian splat frame time, and the levers (render/format/algorithmic) that circumvent them. Distinct from the `quality` target: quality/compression is one lever on performance, not the whole picture — this target also covers render-scale, culling, sort cadence, overdraw-reduction experiments, and (new this session) splat-count reduction via LOD.

Not concerned with the WorldLabs API client or asset-import UX — purely rendering-path performance.

## Why
25fps measured vs. 70fps target on Quest 3. Root cause per `docs/GaussianSplat_Analysis.md`: the renderer was designed for desktop (RTX 3080 Ti class) and is bandwidth/overdraw-bound on Adreno 740's TBDR architecture, not compute-bound (ALU utilization never exceeds 28% even in the most saturated traced config; CPU utilization sits at 13% during the GPU-bound baseline — CPU has substantial idle headroom the GPU does not).

## Where
**Renderer/pipeline (existing bottleneck levers):**
- `Runtime/GaussianSplatting/GaussianSplatRenderer.cs` — per-asset params: `m_ContributionCullThreshold`, `m_AlphaDiscardThreshold` (range now 0-1, was 0-0.5), `m_OpaqueExperiment`, `m_DepthProximityExperiment`, `m_SortNthFrame`. `m_OptimizeForQuest` removed entirely (dead weight — recomputed centerClipPos redundantly, unrelated to actual Quest perf; confirmed via full vertex-shader read). `GatherSplatsForCamera` does CPU object-level frustum culling. `CalcViewData` uploads `_FrustumPlanes` per dispatch for the GPU-side per-splat cull, and (new) `_HotspotCount`/`_HotspotPositions`/`_HotspotFullRadius`/`_HotspotAttenuationRadius` (up to 8) from `GaussianHotspotVolume.ActiveHotspots` each frame for hotspot-driven opacity LOD, skipped when count is 0. Stereo XR: `CalcViewData`'s mono `matView`/`matProj` (from `cam.worldToCameraMatrix`/`GL.GetGPUProjectionMatrix`) are NOT used for the compute-side clip position — that stays on `UNITY_MATRIX_VP` specifically because it resolves correctly per-eye during stereo instanced rendering; `_MatrixVP` (the mono constant) is retained only for the frustum-plane cull and other explicit non-per-eye-sensitive uses.
- `Runtime/GaussianSplatting/GaussianSplatURPFeature.cs` — `resolutionScale` (dominant lever, RS² pixel scaling), `stencilOverdrawCap` (eliminated, no gain), `depthProximityTransparency`, `proximityDepthRange`.
- `Runtime/GaussianSplatting/GaussianHotspotVolume.cs` — new. MonoBehaviour, self-registers to a static list via OnEnable/OnDisable. Fields `m_FullDetailRadius`/`m_AttenuationRadius`. Editor gizmo (solid+wire spheres). Drives per-splat opacity-threshold LOD: full detail inside `m_FullDetailRadius`, threshold ramps to full-cull at `m_AttenuationRadius`, union semantics across multiple hotspots (closest wins, not additive).
- `Shaders/SplatUtilities_DeviceRadixSort.compute` — `CSCalcViewData` does a per-splat bounding-sphere-vs-frustum cull (world-space plane-vs-sphere test against `_FrustumPlanes`), independent of the pre-fork `behindCam` variable. Sort kernels (`CSSetIndices`, `CSCalcDistances`) still dispatch over the full `_SplatCount` unconditionally — the per-splat cull runs too late in dispatch order (after the sort) to reduce their cost; this is the still-open frustum-cull compaction thread. `HotspotCullThreshold()` — per-splat contribution threshold from nearest-hotspot distance ramp, union across up to 8 hotspots; still has an unresolved single-hotspot-no-effect bug (see When). `_HotspotDebugVisualize` — debug toggle, colors splats green→red by nearest-hotspot ramp fraction for direct GPU-side inspection of the bug.
- `Editor/GaussianSplatting/GaussianSplatRendererEditor.cs` — Render Options section (Data Asset → Resources block) partially converted from hand-coded `PropertyField` calls to `foreach` loops over property arrays; still hardcodes field names (not true `SerializedProperty.NextVisible` iteration — declined as incomplete, known stopgap). This conversion incidentally fixed an unexplained bug where `m_HotspotDebugVisualize`'s PropertyField silently failed to render in the Inspector (root cause still unknown — worth checking other unconverted sections if the same silent-non-render pattern resurfaces).
- `Editor/GaussianSplatting/GaussianSplatSeparator.cs` — per-split asset bounds (`boundsMin`/`boundsMax`) now correctly derived from each split's own selected splat positions, not copied verbatim from the source asset (was a real bug: every `_separatedN` output previously shared the original's full bounds, defeating any bounds-dependent behavior for split assets).
- `docs/GaussianSplat_Analysis.md` — full bottleneck hierarchy, GPU counter reference, hardware architecture background.
- `docs/GaussianSplat_Progress.md` — open threads: `[EZ-01]` (true early-Z, blocked on vertex-side NaN cull + tight quad sizing), `[DP-02]` (compute-side pre-binning depth-proximity cull), `[SC-01]` (stream compaction ordering artifact), `[BC7-01]` (Low/VeryLow quality broken on Quest Vulkan), `[CAM-01]`, `[CUT-01]`, `[FX-01]`, `[KOM-01]` (target 200-250k splats for KOM).

**LOD reference (external, read-only, not part of this repo):**
- `D:\Dev\playground\spark\src\SparkRenderer.ts` — CPU-side (Web Worker) LOD tree traversal, screen-space-error + splat-budget driven index selection. `defaultSplatTarget()` (line ~1122) hard-codes 500,000 splats for Oculus/Quest specifically.
- `D:\Dev\playground\spark\src\SplatPager.ts`, `worker.ts` — chunk streaming and (unread) tree-traversal internals.

**Test scene:**
- Unity scene `GaussianPerformanceTests` (project `TechTests`, Unity MCP port 7890) — `Splat/BaseLine` (`burning house daylight.asset`, 262,144 splats, Medium quality, small interior bounds) and `Splat/BadPerformance` (`01-02_LoRes - Red trees on rolling hills-2.asset`, 272,669 splats, VeryHigh/Float32 quality, large landscape bounds ~157×59×171 world units — ~25x BaseLine's linear extent). A `_low.asset` sibling variant (same splat count/bounds, Norm11/Norm6/Norm8x4/Cluster16k format) exists on disk but is not yet assigned in-scene.
- Unity scene `SplatCulling` — Piranesi_500k and its `_separatedN` splits (18-way, correct distinct bounds post-separator-fix) and the unsplit 500k "Original", used to verify the frustum-cull work this session (corridor camera drive showed cut% swinging 49–99.7% correctly with camera framing).

## When
- Session 2 (2026-07-02): Corrected an orientation error: Pass 1 (the TBDR-correct opaque/early-Z path flagged last session as "ready for promotion") is actually visually non-viable — its frag shader computes the gaussian falloff alpha but never applies it, hardcoding full opacity, producing flat colored quads instead of circles; the "no discard needed" comment didn't justify dropping the falloff multiply too. Promotion is now blocked on fixing that, not ready. Started frustum-cull compaction (relocate the existing per-splat cull earlier, shrink dispatch counts) but hit an architectural fact first: neither sort backend (`GpuSorting.cs`, DeviceRadixSort or FidelityFX) uses `DispatchIndirect` — dispatch counts are fixed CPU-side arithmetic, so shrinking them requires either converting both sort backends to indirect dispatch (correct, nontrivial) or a CPU `AsyncGPUReadback` with one frame of lag (simpler, small temporal-coherence cost). Not yet chosen — paused when the cull work already landed (session 1's commit) turned out to have introduced a standalone/Quest-only regression: a black-hole/warping visual effect, present in Android/OpenXR stereo builds but not in the editor (confirmed not a graphics-API issue — reproduces on Quest even when the editor is forced to Vulkan). Root-caused by reading the actual commit diff (`a8f2625`) rather than assuming: `CSCalcViewData`'s clip-position computation had been swapped from `UNITY_MATRIX_VP` (Unity's ambient matrix, correct per-eye during stereo instanced rendering) to `_MatrixVP` (an explicit C# constant uploaded once per frame from a single mono `Camera`, same value for both eyes) — done to fix an unrelated, real SceneView/GameView multi-camera cross-talk problem, without considering the stereo-XR implication. Reverted that one line back to `UNITY_MATRIX_VP`; user verified on-device that the warping is gone and cull behavior is unaffected; committed. Built and landed hotspot-driven opacity LOD end-to-end (`GaussianHotspotVolume` component, GPU distance-ramp cull threshold, C# per-frame upload) as a new perf lever beyond the existing flat contribution-cull threshold — lets specific world regions get more/less aggressive splat culling by proximity to designer-placed volumes. Hit and partially debugged a real bug where only one of two active hotspots had any effect on its region; three independent formula-level fixes were each tried and rejected by play-mode testing, so the union math itself was fully reverted to original — the bug is confirmed NOT in `HotspotCullThreshold`'s ramp/union logic, still open, next step is verifying actual GPU-bound uniform state (not just the CPU-side list) via the debug color-visualization toggle added for this purpose. Committed hotspot LOD + debug viz + an incomplete editor Inspector loop-conversion stopgap together (`06fc42d`). Separately removed `_OptimizeForQuest` entirely after confirming via full shader read it was dead weight (redundant recompute, not an actual Quest optimization) — not yet visually verified. Raised `m_AlphaDiscardThreshold`'s range to 0-1 (was 0-0.5) for more extreme lever testing; user's own extreme-setting profiling found sorting is the dominant cost at high threshold values (17-21ms w/ 24 stale frames at threshold=1), prompting a request for a general kernel-stub framework to isolate each render-path stage's cost — scoped (CSSetIndices/CSCalcDistances/CSCalcViewData + DeviceRadixSort kernels) but not yet built.
- Session 1 (2026-07-01): Oriented on the bottleneck model (overdraw/bandwidth primary, sort/CalcView floor secondary). Investigated a compression-tier anomaly (BadPerformance's "Low made no difference" observation) — resolved as an untested claim: the Low asset was never actually reassigned in-scene; a proper VeryHigh-vs-Low pair now exists on disk for a future on-device test. Theoretically reasoned (no Quest access this session) that Low should meaningfully outperform VeryHigh given the documented bandwidth-bound model. Investigated spark.js's LOD system as a candidate new lever: CPU-worker tree traversal, screen-space-error + hard splat budget (500k on Quest/Oculus specifically) produces a per-frame index subset the GPU renders. Resolved the key feasibility question: spark's worker runs on CPU, which our own traces show is idle (13%) while GPU is the bottleneck — Unity's C# Job System / background thread is the direct equivalent. Did a semantic TBDR-vs-IMR read of the actual pipeline code (not spark-comparison) and found 4 concrete, code-grounded strategy mismatches: (1) production fragment path uses alpha-blend+discard (Pass 0, disables Adreno early-Z) while an already-TBDR-correct opaque/early-Z path (Pass 1, ~19ms overdraw saving per its own comment) sits unused; (2) no spatial culling existed at object or per-splat level — **fixed this session** at both levels; (3) the sort dispatches over the full splat count regardless of culling, unaffected by (2)'s fix since the per-splat cull runs after the sort in dispatch order; (4) the per-splat contribution-cull check (cheap, opacity-only) runs after the kernel's most expensive work (covariance + full SH evaluation) instead of before it. Along the way, found and fixed a real bug in `GaussianSplatSeparator.cs` (per-split bounds were never recomputed, silently defeating any bounds-dependent behavior for split assets). Re-derived the LOD port plan in light of the new cull work: the per-splat frustum test's plane-vs-sphere math is reusable, but doesn't by itself fix mismatch (3) since it runs too late; reframed LOD as two sequential future activities — frustum-cull compaction (move the cull earlier, produce a compacted index buffer, reduce dispatch count for `CSCalcDistances`/sort/`CSCalcViewData` — fixes mismatch (3) on its own, no LOD-specific concepts needed) first, then LOD as "swap the selection source" on top of that plumbing.

Open threads:
- [>] On-device test pending Quest access: assign `01-02_LoRes...rolling hills-2_low.asset` to a scene GameObject and measure actual fps against the VeryHigh original, to confirm the theoretical bandwidth-model prediction (Low should be faster).
- [>] Promote Pass 1 (opaque/early-Z shader path in `RenderGaussianSplats.shader`) to production — corrected in session 2: NOT ready as-is. Its frag shader computes the gaussian falloff alpha but never applies it (hardcodes full opacity), producing flat colored quads instead of circles. Needs the alpha application fixed and revisual-verified before promotion, on top of the still-unconfirmed on-device ~19ms saving claim.
- [>] Frustum-cull compaction: move the per-splat frustum test earlier in dispatch order (before `CSSetIndices`), produce a compacted index buffer, resize `CSCalcDistances`/sort/`CSCalcViewData` dispatch calls to the compacted count. Suspended mid-session-2 (not abandoned) — blocked on choosing between converting both sort backends (`GpuSorting.cs`) to `DispatchIndirect` (correct, nontrivial, touches two third-party-derived implementations) vs. a CPU `AsyncGPUReadback` of survivor count with one frame of dispatch-sizing lag (simple, small temporal-coherence cost, same tradeoff as spark's approach). Neither backend currently uses indirect dispatch at all. Prerequisite groundwork for LOD.
- [>] LOD, built on frustum-cull compaction: CPU-side hierarchy + screen-space-error + splat-budget selection replacing/supplementing the frustum test's compacted index list, once the pipeline is proven dispatch-count-elastic.
- [>] Contribution-cull reordering in `CSCalcViewData`: move the opacity check (`col.a < _ContributionCullThreshold`) before covariance/SH evaluation instead of after — opacity is known immediately from `LoadSplatData`, a small mechanical fix.
- [>] Cold-start `AsyncGPUReadback` garbage-value artifact on the `[CullCounter]` debug log (first frame after scene load reads uninitialized buffer memory before the clearing/dispatching command buffer executes) — self-corrects immediately, not blocking, not addressed.
- [>] Hotspot union bug: with two active `GaussianHotspotVolume`s, only one has any effect on its region (not a math cancellation — the other is a total no-op). Confirmed NOT in `HotspotCullThreshold`'s ramp/union formula — three independent fix attempts (fraction-union, distance-then-lerp-once, direct-smoothstep-to-2.0) were each tried and rejected by play-mode testing; function fully reverted to original. Suspected upstream now — upload/binding issue (what's actually bound on the GPU per-dispatch across the test scene's 19 separate `GaussianSplatRenderer` instances), not shader math. The `m_HotspotDebugVisualize` green→red distance-gradient toggle exists specifically to diagnose this and is now visible in the Inspector (was blocked by an unrelated Inspector-rendering bug, incidentally fixed) — still not yet exercised in play mode with both hotspots active.
- [>] Curve-bias control on `GaussianHotspotVolume` (slider to shape the 0-1 ramp distribution across the full→attenuation band, currently a fixed smoothstep) — queued, deferred until the union bug above is resolved.
- [>] Kernel-stub framework: independently disable any render-path kernel's GPU dispatch (not a pass-through stub) to isolate each pipeline stage's cost — scope identified (`CSSetIndices`, `CSCalcDistances`, `CSCalcViewData`, DeviceRadixSort set, all dispatched together via `SortPoints()`), not yet built. Motivated by user's own profiling finding sorting is the dominant cost at high `ContributionCullThreshold` (17-21ms w/ 24 stale frames at threshold=1; full `AlphaDiscard` gives 40-60ms, worst observed; `OpaqueExperiment` doesn't render on Quest at all).
- [>] `_OptimizeForQuest` removed entirely (shader/C#/editor) after confirming via full read it was dead weight, not an actual optimization — not yet visually/on-device verified post-removal.
- [X] Decompression-overhead-at-Low theory (Low's smaller footprint offset by extra unpack ALU cost) — reasoned against via the bandwidth-bound model (14x bytes/splat reduction vastly outweighs a few cheap unpack ops) and the "no difference" observation that motivated it was later found to be a false test (asset never reassigned) — deprioritized, not proven false, revisit only if on-device data contradicts.
- [X] Vulkan-in-editor as a fast local repro for the stereo XR warping regression — ruled out: confirmed editor was genuinely running Vulkan (API-level check) but still could not reproduce. The regression needs actual Quest/Android stereo XR (OpenXR), not just the Vulkan graphics backend — the differentiator was per-eye stereo matrices, not the API.
- [X] SceneView/GameView cross-talk fix (the original, real reason `_MatrixVP` was swapped in for `UNITY_MATRIX_VP` in `CSCalcViewData`) — deprioritized as editor-only QoL, not load-bearing; the revert back to `UNITY_MATRIX_VP` was taken anyway and verified not to reintroduce a blocking problem. Still an open (unfixed) editor-only issue if it resurfaces.
