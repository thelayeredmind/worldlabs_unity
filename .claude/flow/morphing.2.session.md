- session opened
- target: morphing — resuming, prior session closed clean (button + invariant fix landed)
- direction: regression verification on GaussianSplatMorpher — symptom: works in KOM_Playground, broken in KitchenOfMemories, error in console
[F] Error: "Compute shader (SplatUtilities_DeviceRadixSort): Property (_SplatChunks) at kernel index (1/2) is not set" — fires right after GaussianSplatMorpher.Setup() completes, inside a GUIUtility:ProcessEvent (SceneView repaint), for Car13(50016)<->House-Burning(262144) pair.
[F] Car13_ asset format: posFormat=Float32 scaleFormat=Float32 colorFormat=Float32x4 shFormat=Float32 (all uncompressed) -> chunkDataRefNull=True. Confirmed deliberate: RuntimeSplatProcessing.cs:106-109 only writes chunk data (useChunks) when any format is non-Float32 (compressed) -- chunk data exists solely to carry per-chunk min/max for dequantization. Uncompressed assets correctly have no chunk data.
[H~] Initial hypothesis: GaussianSplatRenderer.OnEnable vs GaussianSplatMorpher.OnEnable ordering race (no [DefaultExecutionOrder] anywhere) -- renderer registers+becomes drawable before morpher's SetExternalBuffers swaps in the chunks==null dummy buffer.
[F] Hypothesis disproven by direct check: both projects have IDENTICAL component order on the GameObject (Transform, GaussianSplatRenderer, GaussianSplatMorpher) -- confirmed via unity_execute_code on both live editor instances (KOM_Playground port 7891, KitchenOfMemories port 7892).
[F] User reproduced the error in KOM_Playground too -- not project-specific. Real differentiator: HighQuality (uncompressed) splats specifically. Compressed-format pairs apparently don't trigger it (unconfirmed, not yet re-tested under controlled conditions).
[D] Next: trace the chunks==null dummy-buffer path directly (GaussianSplatRenderer.SetExternalBuffers lines 553-567, dummy buffer = 1x GaussianSplatAsset.ChunkInfo-sized Raw buffer, m_GpuChunksValid=false) through to SetAssetDataOnCS (line 944) and the CalcDistances/CalcViewData dispatch sites (kernel indices 1 and 2) to find why the dummy buffer doesn't satisfy the compute shader's _SplatChunks bind requirement when uncompressed assets are used.
[^] Continue morphing. Last: disproved OnEnable-ordering race; isolated HighQuality/uncompressed splat format as the actual trigger. Next: trace SetExternalBuffers' chunks==null dummy-buffer branch through SetAssetDataOnCS to CalcDistances/CalcViewData dispatch (kernel 1/2) to find why _SplatChunks isn't considered "set" by the compute shader despite the dummy buffer being bound. Confirm: none.
[F] Root cause confirmed: GaussianSplatRenderer.cs:553 `if (m_GpuChunks != chunks)` guards the dummy-chunk-buffer creation block (lines 553-567). m_GpuChunks is a plain field defaulting to null (line 329). On a freshly-enabled renderer+morpher pair using an uncompressed (HighQuality) source asset, the first SetExternalBuffers call passes chunks=null (since m_BufChunksA/B is null per RuntimeSplatProcessing.cs's useChunks=false for all-Float32 assets). null != null evaluates false, so the swap block is skipped entirely -- m_GpuChunks stays null forever, m_GpuChunksValid never gets set, and CalcDistances/CalcViewData's SetComputeBufferParam(..., Props.SplatChunks, m_GpuChunks) binds an actually-null buffer, producing the "_SplatChunks not set" compute shader error. Not a project-specific race -- reproducible in either project whenever the morpher's first-ever bind happens with both m_GpuChunks and chunks null.
[D] Fix: change the guard at line 553 to also trigger dummy-buffer creation when m_GpuChunks is null and chunks is null (i.e. don't rely on reference-inequality alone -- explicitly handle the "never initialized + no chunks provided" case).
[Task] In GaussianSplatRenderer.cs SetExternalBuffers (lines 520-581), fix the chunk-buffer swap guard at line 553 (`if (m_GpuChunks != chunks)`) so the dummy 1-entry Raw buffer (lines 562-567) is still created when m_GpuChunks is currently null AND the incoming chunks parameter is null -- currently this case is skipped because null != null is false, leaving m_GpuChunks permanently null and causing "Compute shader: Property (_SplatChunks) ... is not set" errors in CalcDistances/CalcViewData whenever a GaussianSplatMorpher's first SetExternalBuffers call happens with an uncompressed (HighQuality, no chunk data) source asset on a renderer whose m_GpuChunks was never previously initialized. Fix: change condition to `if (m_GpuChunks != chunks || m_GpuChunks == null)` (or equivalent explicit null-check), so the existing dummy-buffer path at 562-567 always runs when m_GpuChunks isn't already a valid non-null buffer. Verify by reproducing the original repro (HighQuality/uncompressed splat pair in GaussianSplatMorpher) and confirming the _SplatChunks error no longer appears in the console.

Task: Fix GaussianSplatRenderer.SetExternalBuffers' null-vs-null guard so the dummy chunk buffer is created when m_GpuChunks was never initialized and the morpher passes null chunks (uncompressed/HighQuality asset case).

Ready to wind up.
[^] Continue morphing. Last: root cause confirmed (null!=null guard skip at GaussianSplatRenderer.cs:553). Task opened and ready to wind up -- fix the guard, then verify by reproducing the HighQuality/uncompressed repro case in-editor. Confirm: none.
[S] Step 1: fix null-vs-null guard in SetExternalBuffers + verify in-editor. Surface: code diff + user editor test.
[C] GaussianSplatRenderer.cs:553 — `if (m_GpuChunks != chunks)` changed to `if (m_GpuChunks != chunks || m_GpuChunks == null)` — forces dummy-chunk-buffer creation when m_GpuChunks was never initialized, fixing the null==null skip that left it permanently null for uncompressed/HighQuality morph pairs.
[R] User: _SplatChunks error is gone, but the splat no longer morphs (visually static / not blending).
[F] Second bug found, distinct from the _SplatChunks fix: GaussianSplatMorpher.cs AccumulateAssetBounds (line 410-422) early-returns at line 413 `if (layer.m_ChunkData == null) return;` -- for uncompressed/HighQuality assets (no chunk data), this means world bounds accumulation does NOTHING for that asset. m_WorldBoundsMin/Max (initialized to float.MaxValue/float.MinValue sentinels at ComputeOutputBoundsAndChunk lines 374-375) never get overwritten when BOTH assets are uncompressed -- the degenerate bounding box gets baked into the synthetic output chunk's position min/max (lines 398-400), corrupting position dechunking for every morphed splat. This explains "error gone but splat doesn't morph" -- visually static/garbage positions from the bad bounds, not a chunk-binding issue at all.
[H] AccumulateAssetBounds presumably reads per-chunk min/max from layer.m_ChunkData because that's the cheapest source of bounds for compressed assets (already computed at import time). For uncompressed assets there's no precomputed per-chunk bounds to read -- bounds must be computed directly from positions (layer.m_PosData, already uploaded/available as raw Float32 data) instead of skipped entirely.
[C] GaussianSplatMorpher.cs AccumulateAssetBounds — early `return` on layer.m_ChunkData==null replaced with a fallback that accumulates from asset.boundsMin/boundsMax (asset-level bounds, always computed at import regardless of compression format) instead of skipping the asset's bounds contribution entirely.
[R] User: still no blending after the bounds fallback fix. Bounds fix did not resolve the symptom -- need to look elsewhere.
[F] Third bug, the real cause of "no blending" visible: SplatMorph.compute's MorphSplats/CopyUnmatchedA/CopyUnmatchedB kernels ALWAYS write _OutPos via EncodeNorm11 (packed uint, 4 bytes/splat, FMT_11) regardless of input format. But GaussianSplatMorpher.BindBuffersForT's blend branch (line 268-271) passes m_SplatFormat = asset Left's ORIGINAL format bits (Float32 posFormat for our uncompressed Car13/House pair, which is 12 bytes/splat, FMT_32F) to SetExternalBuffers. Renderer decodes the output position buffer using the format it's told (Float32/12-byte stride) but the actual bytes are Norm11/4-byte packed -- complete format+stride mismatch. This explains the near-constant decoded norm.x (~0.54) readback across very different t values: reading raw Norm11 bit-packed uint bytes as if they were 3 floats produces near-garbage that happens to decode to a similar-looking small range, not real position movement.
[H] Confirmed evidence: console readback log showed norm=(0.543,0.266,0.257) at t=0.38, norm=(0.539,0.264,0.324) at t=0.15, norm=(0.545,0.266,0.235) at t=0.46 -- x and y barely move (0.539-0.545, 0.264-0.266) across wildly different t, consistent with decoding fixed/wrong-stride bit patterns rather than true interpolated positions. z does vary more (0.235-0.324) which is plausible noise from reinterpreting different byte offsets at different strides.
[D] Fix direction: m_SplatFormat passed to SetExternalBuffers in the blend branch must describe the OUTPUT buffer's actual encoding (always Norm11 position, packed rotation+scale per OtherStride(scaleFmtA)) -- not pass through Asset Left's original posFormat unmodified. Need a dedicated "morph output format" constant matching what SplatMpph.compute's kernels actually write, independent of source asset formats.
[C] GaussianSplatMorpher.cs BindBuffersForT blend branch — outSplatFormat now overrides the position-format bits to VectorFormat.Norm11 (matching what SplatMorph.compute's kernels actually write via EncodeNorm11), keeping scale/SH format bits from m_SplatFormat (asset Left's scaleFormat/shFormat, unchanged since Other/SH output still uses those). Previously passed m_SplatFormat unmodified, which carried Left's source posFormat and caused the renderer to decode the Norm11-packed output buffer with the wrong format/stride.
[R] User: format fix changes symptom again -- now entire screen flashes beige during transition (t between 0 and 1), instead of static/no-blend.
[R] User: position fix changes symptom again -- entire screen flashes beige during transition (t between 0,1), not static-but-no-blend anymore. Progress (positions now genuinely move) but new corruption.
[F] Fourth bug, same class as the position one: SplatMorph.compute's Other buffer scale field is ALSO always written as EncodeNorm6 (4-byte .Store at offset+4) regardless of scaleFmtA, but outOtherStride = OtherStride(scaleFmtA) (e.g. 16 bytes for Float32: 4 rot + 12 scale) is used as the per-splat write/allocation stride. Only the first 4 of those 12 scale bytes get real Norm6 data; remaining 8 bytes are uninitialized garbage. Renderer (told scaleFormat=Float32 via the same m_SplatFormat passthrough bug) reads garbage as 3 floats for scale -- explains the beige full-screen blowout (huge/garbage splat scales).
[F] Verified via AllocateOutputBuffers (line 523-551): m_BufOutOther is allocated at otherStride = layerA.m_OtherData byte length / nA -- i.e. ALREADY sized to A's full source stride (16 bytes/splat for Float32+rot). So fixing this is bandwidth-neutral-or-better: the buffer already pays for the full stride; shrinking to the tight actual layout (4 rot + 4 Norm6 scale = 8 bytes/splat) REDUCES both allocation size and per-frame dispatch/copy bandwidth versus today's wasteful over-allocated-but-half-garbage buffer.
[D] Fix direction (user-approved): shrink m_BufOutOther's layout to a fixed 8 bytes/splat (4 rot + 4 packed Norm6 scale) everywhere -- kernel writes, allocation sizing, and the format declared to the renderer (scale format = Norm6, alongside the already-fixed pos format = Norm11). Removes format-passthrough dependency entirely for the output buffer; also reduces GPU memory/bandwidth versus current over-allocated 16-byte stride.
[C] SplatMorph.compute — OUT_OTHER_STRIDE=8 constant added; MorphSplats/CopyUnmatchedA/CopyUnmatchedB's outOtherStride (previously OtherStride(scaleFmtA), source-format-dependent) all switched to this fixed constant, since the output Other layout (rot 4 + Norm6-packed-but-4-byte-aligned scale) never varies with source format.
[C] GaussianSplatMorpher.cs AllocateOutputBuffers — posStride/otherStride changed from source-derived (layerA.m_PosData/m_OtherData byte length / nA) to fixed constants (posStride=4, otherStride=8) matching the morph kernels' actual fixed output layout. shStride still derives from A's source (SH is a passthrough copy, format-dependent).
[X] Attempted: declare output scale format as VectorFormat.Norm6 to the renderer to match the real Norm6-packed bytes. Doesn't work as-is -- renderer's generic Norm6 stride table assumes TIGHT 2-byte packing (otherStride=4+2=6, non-4-aligned, cross-word read-modify reads), but our kernel writes scale at a full 4-byte-aligned slot per splat (stride=8, low 16 bits meaningful, high 16 bits always zero). Renderer would compute otherAddr=idx*6 while actual data is at idx*8 -- same stride-mismatch bug class, one level deeper.
[D] Rejected: implementing true tight 6-byte-stride packing in the compute shader (matching renderer's native non-aligned cross-word Other layout) -- requires read-modify-write logic across 3 kernels for non-aligned RWByteAddressBuffer stores, parity-dependent addressing. Too risky/complex for the bandwidth savings (2 bytes/splat) given no test harness to validate bit-exact packing.
[D] Approved: keep the simple fixed-8-byte-per-splat Other layout (rot 4-byte-aligned + scale 4-byte-aligned-but-16-bit-meaningful). Add renderer-side accommodation for this specific stride instead of forcing it through the generic per-source-format stride table.
[C] GaussianSplatting.hlsl — added VECTOR_FMT_6_PADDED=4 constant (morph-output-only, never a real asset import format). LoadAndDecodeVector's VECTOR_FMT_6 branch extended to also match VECTOR_FMT_6_PADDED (same DecodePacked_6_5_5 bit layout works for both since addrU is always 4-aligned for the padded case anyway). LoadSplatData's otherStride computation adds a VECTOR_FMT_6_PADDED branch (+4 instead of +2) so otherAddr matches the morph's actual fixed 8-byte-per-splat layout.
[C] GaussianSplatMorpher.cs BindBuffersForT blend branch — outSplatFormat now explicitly builds posFormat=Norm11(2) | scaleFormat=Norm6Padded(4)<<8 | shFormat passthrough from m_SplatFormat, using raw uint constants rather than GaussianSplatAsset.VectorFormat enum members (VECTOR_FMT_6_PADDED=4 deliberately NOT added to that C# enum since it's never a real serialized asset format, just an internal renderer/shader signaling value for this one runtime-only code path).
[D] Verified GetSplatFormat() (GaussianSplatRenderer.cs:741-746) returns m_ExternalSplatFormat uniformly for both the compute (SetAssetDataOnCS) and material (SetAssetDataOnMaterial) paths when HasExternalBuffers -- single source of truth, no risk of compute/render seeing different format bits.
[R] User: new test with different asset pair (The Gears Awaken_500k <-> Piranesi_500k, 500k+500k, also uncompressed -- chunksA/chunksB blank in log). New error: "Compute shader (SplatMorph): Property (_ChunksA) at kernel index (0) is not set". outPos[0] readback is FROZEN identical (enc=578670434) across t=0.75, 0.42, 0.51 -- dispatch appears to be a no-op/failing silently after the property-not-set error, not actually recomputing positions.
[F] Fifth bug, same null-binding-skip class yet again: DispatchMorph's `if (m_BufChunksA != null) SetBuffer(...)` (and B, and the two CopyUnmatched kernel variants) NEVER bind _ChunksA/_ChunksB when the asset is uncompressed -- but the shader unconditionally declares ByteAddressBuffer _ChunksA/_ChunksB and Unity's compute shader validation requires every declared resource to have something bound, even though Dechunk()'s chunkCount==0 runtime branch never actually reads from it. This produced "Property (_ChunksA) at kernel index (0) is not set" and the dispatch appears to silently no-op (frozen outPos readback across very different t values) when validation fails.
[C] GaussianSplatMorpher.cs DispatchMorph (MorphSplats kernel + CopyUnmatchedA/CopyUnmatchedB kernels, 3 call sites) -- `if (m_BufChunksX != null) SetBuffer(...)` changed to always SetBuffer, falling back to m_BufPosA/m_BufPosB (any already-valid buffer of the right type) as a dummy binding when there's no real chunk buffer. chunkCountA/chunkCountB stay 0 so the dummy is never actually read by Dechunk's early-return guard -- purely satisfies Unity's "must bind something" validation, same pattern as the renderer-side dummy buffer fix from earlier in this task.
[R] User: confirmed fixes work. Earlier "everything went to shit" message was unrelated (different chat, scene/lighting reset matter) -- not a regression from our changes. Review loop closed.
[/S]
[/Task]
[^] Continue morphing. Last: review clean -- all 5 chained bugs fixed and confirmed working by user (chunk dummy-buffer null-guard, world bounds fallback for uncompressed assets, output position format declaration, output scale format+stride, compute shader chunk binding). Next: drift or wrap-up. Confirm: none.
[CKP] User committed -- "fix: support uncompressed (HighQuality) splat assets in GaussianSplatMorpher" (user handled staging/commit themselves, not Claude).
[D!] User does not want Claude verifying their git actions (status/log checks) after they state something is done -- stated as "none of your business" when Claude ran git status to confirm a commit landed. Take user's word at face value going forward; do not independently re-verify completed user actions unless asked.

--- CLOSED 2026-06-22 — fixed five chained bugs blocking uncompressed/HighQuality splat assets in GaussianSplatMorpher (chunk dummy-buffer null-guard, world bounds fallback, output position/scale format+stride declarations, compute shader chunk binding) ---
