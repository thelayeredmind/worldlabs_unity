- session opened
- target: morphing — feature already built, exploring next steps
- regression to verify on GaussianMorpher, but wants UX improvement done first
- UX idea: GaussianMorpher needs a MorphTarget; currently only creatable via menu. Want second path: button in inspector next to target selection fields, auto-populates the creation window, converges into the existing main creation path.
[>] Regression verification on GaussianMorpher — parked until UX improvement lands.
[F] "MorphTarget" = GaussianMorphMap asset. "GaussianMorpher" = GaussianSplatMorpher (Editor/GaussianSplatting/Morph/GaussianSplatMorpherEditor.cs). Menu creation path = Tools/Gaussian Splats/Build Morph Map, opens GaussianMorphMapBuilderWindow (Editor/GaussianSplatting/Morph/GaussianMorphMapBuilderWindow.cs), static Open() with no params, fields m_AssetLeft/m_AssetRight/m_OutputFolder/m_ColorWeight are private SerializedField, no public API to pre-populate.
[D] Button always visible next to Asset Left/Right fields (not conditional on missing-map state) — separate, permanent second creation path rather than augmenting the existing warning HelpBox.
[D] Button disabled until both Asset Left and Asset Right are set — mirrors GaussianMorphMapBuilderWindow's own DisabledScope rule (line 113). Once both set, button enables and opens window pre-filled with both.
[D] Button label: "Build Morph Map…" (matches menu item / window's internal button wording, not the asset type name).
[Task] In GaussianSplatMorpherEditor.cs, add a button labelled "Build Morph Map…" placed near the Asset Left/Right fields (after the Swap button block, ~line 57-59, before the MorphMap section). Disabled (via EditorGUI.DisabledScope) unless both m_AssetLeft and m_AssetRight are set, mirroring GaussianMorphMapBuilderWindow's own build-button gating (line 113). On click, open GaussianMorphMapBuilderWindow pre-populated with the morpher's assetLeft/assetRight. This requires adding an overload to GaussianMorphMapBuilderWindow.Open() (currently parameterless, line 45-51) that accepts left/right GaussianSplatAsset and pre-fills m_AssetLeft/m_AssetRight before showing. The window's existing StartBuild()/SaveAsset() path is reused unchanged — this is purely a second entry point converging into the same creation flow. Goal: from the GaussianSplatMorpher inspector, with both assets already chosen, one click opens the builder window ready to build, instead of requiring the user to navigate Tools → Gaussian Splats → Build Morph Map and re-pick the same two assets.

Task: Add inspector button "Build Morph Map…" next to GaussianSplatMorpher's asset fields, opening a pre-populated Morph Map Builder window.

Ready to wind up.
[S] Step 1: Open() overload on GaussianMorphMapBuilderWindow + inspector button on GaussianSplatMorpherEditor — full feature in one step. Surface: code diff.
[C] GaussianMorphMapBuilderWindow.Open() — was a single parameterless static method with inline window-creation code; split into Open()/Open(left,right) overloads delegating to a shared private OpenWindow(left,right), per user's inline review comment flagging duplicated hard-coded Rect/minSize values.
[/S]
[/Task]
[CKP] 80bfc74 — "feat: add inspector button to open Morph Map Builder pre-filled with morpher's assets" — user committed directly.
[F] Original regression does not reproduce in this project — everything works fine on direct test.
- New finding: in the Debug Points view, duplicate splats are visible — appear to be invisible counterparts of splats from the other morph side, overlapping/coincident rather than truly hidden.
[H] Unmatched splats (GaussianSplatMorpher.cs DispatchMorph passes 2/3) are faded via opacity ×(1−t) / ×t rather than removed — they always occupy a slot in the output buffer. Debug Points render mode (RenderMode.DebugPoints, m_MatDebugPoints) likely draws raw point positions and may not respect per-splat alpha/opacity the way the main splat shader does. If so, splats meant to be "invisible" at extreme t are still drawn as solid points in Debug Points view — visible as duplicates near/at their matched-pair counterpart's position. Unverified — need to check the Debug Points shader/material for opacity handling.
[F] Confirmed by user: unmatched splats keep their fixed source position (never moved, only faded via opacity) — Debug Points ignores alpha, so they render as static "stale" duplicates instead of appearing to fade. Not a bug in morph logic — expected behavior of Debug Points combined with the fade-not-move design of unmatched splats.
[D] Debug Points duplicate-look was just context, not an action item — DebugPoints stays as-is. Parked: optimizing MorphMap to minimize unmatched splats.
[>] Optimize MorphMap correspondence to minimize unmatched splat count.
[Task] Verify that GaussianSplatMorpher's total morph output splat count (m_TotalMorphCount = matchedCount + unmatchedLeftCount + unmatchedRightCount, set in BuildIndexBuffer() at GaussianSplatMorpher.cs:493) never exceeds max(assetLeft.splatCount, assetRight.splatCount). The union of matched pairs + both unmatched sets should logically equal at most the larger source set (matched pairs are shared, only genuinely unmatched splats from each side should add on top) — if total exceeds max(a,b), that indicates double-counting or a correspondence-building defect in GaussianMorphMapBuilder. Goal: confirm invariant holds (or find where/why it's violated) for representative asset pairs, particularly ones with differing splat counts.

Task: Verify GaussianSplatMorpher's total output splat count never exceeds max(splatCountLeft, splatCountRight).

Ready to wind up.
[F] Invariant analysis: every L splat ends matched or in unmatchedLeft, every R splat ends matched or in unmatchedRight (GaussianMorphMapBuilder.Build/ResolveOneToOne). So total = nL + nR - matchedCount. total <= max(nL,nR) only holds if matchedCount >= min(nL,nR), i.e. every splat on the smaller side gets matched — not guaranteed by ResolveOneToOne's collision handling across kMaxMatchRounds=8 rounds.
[F] Verified by simulation (ported algorithm exactly to JS, ran 5000 random trials, nL/nR in [1,30]): 30/5000 (0.6%) violate total<=max(nL,nR) — e.g. nL=27 nR=29 matched=26 uL=1 uR=3 total=30>29. Collisions unresolved after 8 rounds leave both-sided unmatched splats, inflating total beyond max(a,b). Invariant does NOT hold in general; gets worse as splat counts grow and collision rate rises.
[/Task]
[D] Verification task closed — invariant confirmed violated. User wants to fix collision resolution so matchedCount >= min(nL,nR) is guaranteed (total never exceeds max(nL,nR)).
[D] Strategy: force-match leftovers after kMaxMatchRounds — once the round loop ends (cap hit or no-progress break) with both remainingL and remainingR non-empty, greedily pair off remaining splats by nearest distance (ignoring the normal one-to-one collision rule), continuing until one side is exhausted. Guarantees matchedCount >= min(nL,nR), so total = nL+nR-matchedCount <= max(nL,nR) always holds. Lower-quality matches accepted for these leftovers — correctness of the invariant prioritized over match quality for the tail.
[Task] In GaussianMorphMapBuilder.Build() (Editor/GaussianSplatting/Morph/GaussianMorphMapBuilder.cs:58-125), after the round loop ends, if both remainingL and remainingR are still non-empty, force-match them by greedy nearest-distance pairing (using existing posL/posR, no need to re-dispatch GPU search — direct distance computation is fine since remainder is small/the tail after normal matching) until one side is exhausted. Append these forced pairs to `pairs`, and set unmatchedLeft/unmatchedRight to whatever remains on the longer side only. This guarantees matchedCount >= min(nL,nR) and therefore total <= max(nL,nR), fixing the union-size invariant verified broken in the prior task ([F] entries above — 0.6% violation rate in simulation, root cause = unresolved collisions left unmatched on both sides after the 8-round cap).

Task: Force-match leftover unmatched splats after GaussianMorphMapBuilder's round cap so matchedCount >= min(nL,nR), guaranteeing total output splat count never exceeds max(splatCountLeft, splatCountRight).

Ready to wind up.
[S] Step 1: implement force-match leftovers in GaussianMorphMapBuilder.Build() + re-run invariant simulation. Surface: code diff + simulation re-run. User smoke test: rebuild MorphMap for an existing asset pair, in Debug Points view no splats should remain stale — ideally all visibly move during morph.
[R] User: in-editor smoke test stuck at 85% — ForceMatchRemainder's O(n²) shrinking-pool brute force is too slow / hanging on real asset sizes.
[D] Strict 1:1 forced pairing is not actually required — goal is convergence (every splat has somewhere to move toward), not uniqueness. Many-to-one matching is acceptable: duplicates allowed.
[D] Replace ForceMatchRemainder's pairwise removal loop with plain nearest-neighbor lookup: each leftover-on-one-side splat finds its nearest splat in the FULL opposite set (matched or unmatched) — no removal, no shrinking pool, O(n*m) one-pass instead of O(n²) with removal each iteration (still O(n*m) per remaining splat but no incumbent-removal bookkeeping or shrinking-pool overhead, and is naturally fast since at most one side has leftovers after the round loop, often a small remainder against the full other-side N).
[C] GaussianMorphMapBuilder.cs — ForceMatchRemainder (shrinking-pool O(n²) pairwise removal) replaced with ResolveRemainderByNearestNeighbor (O(n*m) nearest-neighbor lookup against full opposite set, no removal, duplicates allowed).
[F] Bidirectionality check (user question): SplatMorph.compute's MorphSplats kernel is one-thread-per-pairs-entry, reading _PosA[pair.x]/_PosB[pair.y] independently per thread and writing to its own output slot — duplicate x or y indices across multiple pairs entries are harmless to the shader (each thread still does a valid, symmetric A↔B lerp for its own pair). So the A↔B lerp relationship (bidirectionality) is preserved per-pair; what's given up is uniqueness (a splat can appear in more than one pair) — consistent with the design decision that convergence matters, not exclusivity.
[F] Re-verified at scale: 3000 trials up to 200x200 — 0 remainder violations, full coverage (every splat in >=1 pair). Synthetic 20k x 20k case (matching the scale that was hanging) builds in ~586ms with the new nearest-neighbor approach — confirms the old O(n²) shrinking-pool was the perf cause of the 85%-stuck hang.
[R] User: still hangs at 85% with the nearest-neighbor rewrite — regression vs. pre-fix behavior (build used to complete, just with the invariant violated).
[H] 85% is logged exactly when the round loop finishes (0.05+0.8*8/8) — my ResolveRemainderByNearestNeighbor runs after that with zero progress reporting, so apparent "hang" could be silent slow grinding rather than deadlock. No actual deadlock logic in the method (no locks/waits) — confirmed by reading the code.
[F] Root cause: ResolveRemainderByNearestNeighbor is O(leftover * full_opposite_side) on a single CPU thread. SplatCorrespondence.compute's FindBestMatch kernel does the exact same O(n*m) brute-force distance search but on GPU across thousands of parallel threads via the existing ICorrespondenceDispatcher — that's why the round-loop matching (same complexity class) stays fast while my CPU remainder pass is slow/looks hung at real asset scale (100k–2M+ splats).
[D] Fix: route the remainder resolution through the same GPU dispatcher (ICorrespondenceDispatcher.FindBestMatches) used by the round loop, instead of a serial CPU loop.
[C] ResolveRemainderByNearestNeighbor (CPU NearestIndex loop) replaced with ResolveRemainderOnGpu — dispatches leftover-vs-full-opposite-set search through the same ICorrespondenceDispatcher.FindBestMatches used by the round loop, reusing its existing main-thread GPU marshalling (MainThreadDispatcher in the window) — same call pattern as the existing round loop at line 97-98, no new threading risk.
[/S]
[/Task]
[CKP] User committed — "fix: guarantee morph output splat count never exceeds max(left,right) splat count" — GPU-dispatched remainder resolution.
[^] Continue morphing. Last: wrap-up complete — context.md written (corrected per user: correspondence is offline due to Quest runtime cost, not splat-count mismatch), CLAUDE.md updated with GPU-dispatch-over-CPU-loop convention.

--- CLOSED 2026-06-19 — added inspector second-creation-path button for Morph Map Builder; fixed morph output splat-count invariant via GPU-dispatched remainder resolution ---
